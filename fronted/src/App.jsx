import React from "react";
import User from "./User"; // Adjust path if in subfolder
import "./App.css"; // Optional: add global styles if needed

const App = () => {
  return (
    <div className="App">
      <User />
    </div>
  );
};

export default App;
